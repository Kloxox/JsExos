valeur1 = -2;
valeur2 = 1;
valeur3 = 4;

let tab = [valeur1, valeur2, valeur3];

//console.log(tab.length);

function additionne(x) {
    let result = x + 2;
    return result;
  }



  
let resultat = additionne(x);
console.log(result);

function affiche(){
    //boucle for sur tab ( i < tab.length ) 
        // pour chaque valeur du tab de i
        // alert ( additionne(tab[i])
        for (let i=1; i<tab.length; ++i){
            elementDiv.onclick = function() {alert("tu as cliqué");};
            alert (additionne(tab[i]));
            System.out.println(i); 

            console.log(func1.length);
        }

        let bouton = document.getElementById("bouton");
        btn.addEventListener("click", function(){
        alert('clicker')});
 
var div = document.getElementById(afficher)
div.add (EventTarget.addEventListener(), affichage())
}

